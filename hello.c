#include <stdio.h>

int main() {
    printf("Hello Git Version 2!\n");
    int x = 0;
    printf("x = %d\n", x);

    int y = 30;
    float avg = (float) (x + y) / 2;
    printf("average = %.2f\n", avg);
    return 0;
}